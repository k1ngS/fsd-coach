{
  "name": "fsd-coach",
  "version": "1.0.0",
  "bin": {
    "fsd-coach": "dist/cli.js"
  },
  "scripts": {
    "build": "tsup src/cli.ts --format esm,cjs",
    "dev": "tsup src/cli.ts --format esm,cjs --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:watch": "tsc -p tsconfig.json --noEmit --watch"
  },
  "dependencies": {
    "@fsd-coach/core": "workspace:*",
    "@inquirer/prompts": "^7.10.0",
    "chalk": "^5.6.2",
    "commander": "^12.1.0"
  }
}
